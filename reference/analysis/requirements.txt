# バージョンを固定する。範囲指定だと CI で最新版が入り、
# ローカルで通ったものが CI で落ちる（issue #8）。
# 更新は Dependabot の PR 経由で行う。
#
# メジャーは上げない（issue #16）。ここは Go 移植の検証基準（ADR-0011）で、
# 「Go の出力がこれと一致すること」を受け入れ条件にしている。
# 基準側の数値が動くと比較そのものが成立しなくなる。
# Python も 3.11 に固定する（reference/.python-version）。
pandas==2.1.4
numpy==1.26.4
