# 個人データ一式（体組成・写真・血液検査・実記録・自分の計画）
private/

# 自動生成
reports/
__pycache__/
*.pyc

# 環境
.env*
!.env.example
.DS_Store

# Go
/api/bin/
*.test
coverage.out

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform.lock.hcl.bak

# Docker
.docker/

# iOS / Web
node_modules/
.next/
next-env.d.ts
.playwright-mcp/
# Next.js が生成するルート型。ビルドのたびに作られる
web/.next/types/
# pnpm を使う。npm/yarn の lockfile が混入しないようにする（CLAUDE.md）
package-lock.json
yarn.lock
*.xcuserstate
xcuserdata/
build/

# Swift / Xcode
ios/.build/
ios/.swiftpm/
DerivedData/
*.xcuserdatap

# iOS の接続先と鍵。Config.xcconfig.example をコピーして作る
ios/Physique/Config.xcconfig
